# Graphic

---

## 1. Environment

### Ubuntu Linux

``` bash
sudo apt-get install libglfw3
sudo apt-get install libglfw3-dev

git submodule init
git submodule update
```

### Arch Linux

``` bash
pacman -S glfw-x11

git submodule init
git submodule update
```